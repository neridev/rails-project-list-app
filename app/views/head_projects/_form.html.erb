<%= form_with(model: head_project) do |form| %>
  <% if head_project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(head_project.errors.count, "error") %> prohibited this head_project from being saved:</h2>

      <ul>
        <% head_project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :head_id, style: "display: block" %>
    <%= form.text_field :head_id %>
  </div>

  <div>
    <%= form.label :project_id, style: "display: block" %>
    <%= form.text_field :project_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
